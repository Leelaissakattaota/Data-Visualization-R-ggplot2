# 📊 Data Visualization with R: ggplot2 & dplyr 🚀

Welcome to my data analysis project! This repository contains a comprehensive R script focused on exploring and visualizing the **Gapminder dataset**. It demonstrates the proficiency in `dplyr` for data manipulation and `ggplot2` for creating professional-grade, insightful visualizations.

---

## 🛠️ Key Technologies Used
* **Language**: R
* **Data Manipulation**: `dplyr`
* **Data Visualization**: `ggplot2`
* **Dataset**: `gapminder` (Global health, population, and wealth data)

---

## 📋 Project Highlights

This project is structured into specific tasks that showcase a variety of data science techniques:

### 🔍 Data Exploration & Summarization
- Filtering and subsetting data for specific timeframes (e.g., comparing 1957 vs 2007).
- Calculating statistical summaries like **Median GDP** and **Median Life Expectancy** using `group_by()` and `summarize()`.

### 📈 Visualization Techniques
- **Scatter Plots**: Analyzing correlations between economic wealth and health outcomes.
- **Logarithmic Scaling**: Using `scale_x_log10()` and `scale_y_log10()` to normalize data distributions for better visibility.
- **Faceting**: Creating small multiples (sub-plots) by Continent to compare regional trends simultaneously.
- **Line Plots**: Visualizing time-series trends to track global progress over decades.
- **Bar Plots**: Using `geom_col()` to compare discrete categories like median GDP by country or continent.
- **Distribution Plots**: Utilizing **Histograms** and **Boxplots** to identify data density, skewness, and outliers.

---

## 🚀 How to Use

1. **Clone the repository**:
   ```bash
   git clone [https://github.com/YOUR_USERNAME/Data-Visualization-R-ggplot2.git](https://github.com/YOUR_USERNAME/Data-Visualization-R-ggplot2.git)

2. **Open the script in RStudio.**
3. **Install dependencies:**
   install.packages(c("ggplot2", "dplyr", "gapminder"))
5. **Run the script to generate the visualizations.:**
   
   
